import * as FeedActions from '@store/feed/actions';
import { FeatureKey, reducer, State } from '@store/feed/reducer';
import * as FeedSelectors from '@store/feed/selectors';

export { FeedActions, FeatureKey, reducer, State, FeedSelectors };
